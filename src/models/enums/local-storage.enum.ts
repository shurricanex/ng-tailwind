export enum LocalStorage {
    token = '_t',
    user = '_u',
    refresh_token = '_rt',
    password = '_p',
    finished_quizzes_ids = '_fqi',
    current_unfinished_quiz = '_cuq',
    theme = 'th'
}
